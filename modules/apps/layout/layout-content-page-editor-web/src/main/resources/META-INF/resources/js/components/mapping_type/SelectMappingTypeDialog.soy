{namespace com.liferay.layout.content.page.editor.web.SelectMappingTypeDialog}

/**
 * SelectMappingTypeDialog
 */
{template .render}
	{@param? _handleVisibleChanged: any}
	{@param? selectMappingTypeDialogVisible: bool}
	{@param? spritemap: string}
	{@param? store: ?}

	{if $selectMappingTypeDialogVisible}
		{call liferay.frontend.Modal.render}
			{param body kind="html"}
				{call com.liferay.layout.content.page.editor.web.SelectMappingTypeForm.render}
					{param store: $store /}
				{/call}
			{/param}

			{param dialogClasses: 'modal-dialog-sm' /}
			{param events: ['visibleChanged': $_handleVisibleChanged] /}

			{param header kind="html"}
				{msg desc=""}select-asset-type{/msg}
			{/param}

			{param spritemap: $spritemap ?: '' /}
		{/call}
	{/if}
{/template}